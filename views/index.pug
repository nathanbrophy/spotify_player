extends layout

block content
  h1= title
  p Welcome to #{title}

  #info_view
    #bus
    #train
    #weather
      #weather_view(style="background:red;") Weather View
        #weather_now(style="background:green;") Weather Now
        #weather_now_img(style="background:blue;") Weather Now Img
        #weather_hourly(style="background:yellow;") Weather Hourly